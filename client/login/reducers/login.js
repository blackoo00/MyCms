import types from '../contants/ActionTypes'

const initialState = {
    warn: ''
}

const App = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default App
